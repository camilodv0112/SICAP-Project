<Window x:Class="Presentacion.Views.PersonalFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:Presentacion.Core"
        mc:Ignorable="d"
        Title="Gestión de Personal" Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <core:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#002898" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Form -->
                <RowDefinition Height="Auto"/> <!-- Error Message -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Registrar Nuevo Personal" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#002898"/>

            <!-- Form -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Column 1 -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Primer Nombre *"/>
                        <TextBox Text="{Binding NuevoPersonal.PrimerNombre, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Segundo Nombre"/>
                        <TextBox Text="{Binding NuevoPersonal.SegundoNombre, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Primer Apellido *"/>
                        <TextBox Text="{Binding NuevoPersonal.PrimerApellido, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Segundo Apellido *"/>
                        <TextBox Text="{Binding NuevoPersonal.SegundoApellido, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Cédula *"/>
                        <TextBox Text="{Binding NuevoPersonal.Cedula, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Número de Empleado"/>
                        <TextBox Text="{Binding NuevoPersonal.NumeroEmpleado, UpdateSourceTrigger=PropertyChanged}"/>
                        
                        <TextBlock Text="Sexo * (M/F)"/>
                        <ComboBox SelectedValue="{Binding NuevoPersonal.Sexo}" SelectedValuePath="Content">
                            <ComboBoxItem Content="M"/>
                            <ComboBoxItem Content="F"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Column 2 -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Celular *"/>
                        <TextBox Text="{Binding NuevoPersonal.Celular, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Correo Electrónico"/>
                        <TextBox Text="{Binding NuevoPersonal.CorreoElectronico, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Dirección *"/>
                        <TextBox Text="{Binding NuevoPersonal.DireccionDomiciliaria, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                        <TextBlock Text="Municipio *"/>
                        <ComboBox ItemsSource="{Binding MunicipiosDisponibles}" 
                                  SelectedItem="{Binding SelectedMunicipio}"
                                  DisplayMemberPath="Nombre"/>

                        <TextBlock Text="Cargo *"/>
                        <ComboBox ItemsSource="{Binding CargosDisponibles}" 
                                  SelectedItem="{Binding SelectedCargo}"
                                  DisplayMemberPath="Nombre"/>

                        <TextBlock Text="Disciplina Artística"/>
                        <ComboBox ItemsSource="{Binding DisciplinasDisponibles}" 
                                  SelectedItem="{Binding SelectedDisciplina}"
                                  DisplayMemberPath="Nombre"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- Error Message -->
            <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancelar" Command="{Binding CancelCommand}" Width="100" Height="35" Margin="0,0,10,0" Background="#E0E0E0" BorderThickness="0">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Guardar" Command="{Binding SaveCommand}" Width="100" Height="35" Background="#002898" Foreground="White" BorderThickness="0">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Loading Overlay -->
            <Grid Grid.RowSpan="4" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Procesando..." Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>
