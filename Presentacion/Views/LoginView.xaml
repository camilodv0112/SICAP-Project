<Window x:Class="Presentacion.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentacion.Views"
        xmlns:vm="clr-namespace:Presentacion.ViewModels"
        xmlns:core="clr-namespace:Presentacion.Core"
        mc:Ignorable="d"
        Title="SICAP - Inicio de Sesión" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#EEEEEE">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <core:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <core:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

        <!-- Estilo para Botones CTA -->
        <Style x:Key="CtaButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#002898"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Verdana"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#204BB2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#C7C7C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para TextBox con Bordes Redondeados y Placeholder -->
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#C7C7C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1">
                            <Grid>
                                <!-- Placeholder Text -->
                                <TextBlock x:Name="placeholder"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#C7C7C7"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                
                                <!-- Content Host -->
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Focusable="false"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Mostrar Placeholder cuando el texto está vacío -->
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <!-- Cambiar borde al enfocar -->
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#002898"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Bienvenido a SICAP" 
                   FontSize="24" 
                   FontFamily="Verdana" 
                   FontWeight="Bold" 
                   Foreground="#111111"
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,40"/>

        <!-- Usuario -->
        <TextBlock Grid.Row="1" Text="Usuario:" 
                   FontFamily="Verdana" 
                   Foreground="#2E2E2E" 
                   FontSize="14"
                   Margin="0,0,0,8"/>
        <TextBox Grid.Row="2" 
                 Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}" 
                 Tag="Ingrese su usuario"
                 Margin="0,0,0,20"/>

        <!-- Contraseña -->
        <TextBlock Grid.Row="3" Text="Contraseña:" 
                   FontFamily="Verdana" 
                   Foreground="#2E2E2E" 
                   FontSize="14"
                   Margin="0,0,0,8"/>
        <!-- Nota: PasswordBox no soporta Binding directo por seguridad. -->
        <TextBox Grid.Row="4" 
                 Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                 Tag="Ingrese su contraseña"
                 Margin="0,0,0,30"/>

        <StackPanel Grid.Row="5" VerticalAlignment="Top">
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       FontFamily="Arial"
                       TextWrapping="Wrap" 
                       Margin="0,0,0,15" 
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            
            <Button Content="Iniciar Sesión" 
                    Command="{Binding LoginCommand}" 
                    Style="{StaticResource CtaButtonStyle}"
                    Height="45"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
            
            <TextBlock Text="Cargando..." 
                       FontFamily="Arial"
                       Foreground="#111111"
                       HorizontalAlignment="Center" 
                       Margin="0,15,0,0"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>
